# Volta Newsletter App — demo-ready out of the box

# Demo Mailchimp (pretend key — full send flow works without a real API key)
DEMO_MODE=true
MAILCHIMP_API_KEY=DEMO_MAILCHIMP_KEY
MAILCHIMP_SERVER_PREFIX=us1
MAILCHIMP_AUDIENCE_ID=demo_volta_community
MAILCHIMP_FROM_NAME=Volta
MAILCHIMP_REPLY_TO=hello@voltaeffect.com

# When you get a real Mailchimp key later:
# DEMO_MODE=false
# MAILCHIMP_API_KEY=your-real-key-here
# MAILCHIMP_SERVER_PREFIX=usX
# MAILCHIMP_AUDIENCE_ID=your-audience-id

# OpenAI (optional — without it, research uses local/community heuristics)
OPENAI_API_KEY=

# App
VOLTA_DATA_DIR=./data/local
# On Fly/Render/Railway: point this at a persistent volume (e.g. /data).
# Without a volume, recommendations + consent are wiped on every redeploy.

# REQUIRED on any public URL — generate with: openssl rand -hex 32
# Leave blank only for local laptop demos (uses an insecure default).
VOLTA_SESSION_SECRET=

# Cadence: monthly (default) | biweekly | weekly
# Monthly is intentional — do not change to weekly without an explicit trial.
NEWSLETTER_CADENCE=monthly

# Slack slash command: /suggest-newsletter → POST /slack/suggest
# Create at https://api.slack.com/apps → Slash Commands → Request URL:
#   https://volta-newsletter.fly.dev/slack/suggest
# Then set Signing Secret here and on the host (fly secrets set SLACK_SIGNING_SECRET=...)
# Requests without a valid signature are rejected — never skip this, even in demo.
SLACK_SIGNING_SECRET=

# Optional shared secret for POST /ingest/email (leave blank for open local demo)
EMAIL_IN_TOKEN=
